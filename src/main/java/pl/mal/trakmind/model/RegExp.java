package pl.mal.trakmind.model;

public interface RegExp {
    String USERNAME = "[a-zA-Z]+";  //only letters
    String PASSWORD = "\\d+";       //only numbers
    String EMAIL = "\\d+";          //only numbers
}
